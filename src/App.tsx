import React, { useEffect, useMemo, useState } from 'react';
import { AssignmentWarning, CurrentShiftState, FinalizedShiftSnapshot, ForecastEvent, MTCoverageState, NurseStaff, OnCallProviders, OperationalEvent, PatientRoom, PCTCoverageState, PlanBaseline } from './types';
import { StorageService } from './services/storage';
import { runRecommendationEngine } from './services/recommendationEngine';
import { MRS_CONFIG, getMRSStatus } from './config/mrs';
import { UnitBoard } from './components/UnitBoard';import { PrintSheet } from './components/PrintSheet';import { NextShiftForecast } from './components/NextShiftForecast';import { RosterPlanner } from './components/RosterPlanner';import { CurrentStaffing } from './components/CurrentStaffing';import { HistoryView } from './components/HistoryView';
import { AlertTriangle, CalendarDays, Clock3, History, Play, Printer, RotateCcw } from 'lucide-react';
const tomorrowIso=()=>{const d=new Date();d.setDate(d.getDate()+1);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;};
const CURRENT_MRS_KEY='1E_CURRENT_MRS_INPUT',PROJECTED_MRS_KEY='1E_PROJECTED_MRS_INPUT',PLAN_DATE_KEY='1E_PLAN_DATE',SHIFT_KEY='1E_SHIFT_TYPE';type Tab='current'|'plan'|'forecast'|'history'|'print';
export const App:React.FC=()=>{const[activeTab,setActiveTab]=useState<Tab>('current'),[date,setDate]=useState(()=>localStorage.getItem(PLAN_DATE_KEY)||tomorrowIso()),[shiftType,setShiftType]=useState<'Day'|'Night'>(()=>(localStorage.getItem(SHIFT_KEY) as 'Day'|'Night')||'Day'),[chargeTakingPatients,setChargeTakingPatients]=useState(false),[allowTeleQuad,setAllowTeleQuad]=useState(false),[mtState,setMtState]=useState<MTCoverageState>('MT_PRESENT'),[pctState,setPctState]=useState<PCTCoverageState>('PCT_PRESENT'),[roster,setRoster]=useState<NurseStaff[]>([]),[rooms,setRooms]=useState<PatientRoom[]>([]),[onCall,setOnCall]=useState<OnCallProviders>(StorageService.loadOnCall()),[forecastEvents,setForecastEvents]=useState<ForecastEvent[]>([]),[warnings,setWarnings]=useState<AssignmentWarning[]>([]),[fitScore,setFitScore]=useState(100),[fitLabel,setFitLabel]=useState('NOT YET GENERATED'),[unassignedRooms,setUnassignedRooms]=useState<string[]>([]),[currentMRS,setCurrentMRS]=useState(()=>Number(localStorage.getItem(CURRENT_MRS_KEY)||MRS_CONFIG.target)),[projectedMRS,setProjectedMRS]=useState(()=>Number(localStorage.getItem(PROJECTED_MRS_KEY)||MRS_CONFIG.target)),[currentShift,setCurrentShift]=useState<CurrentShiftState>(()=>StorageService.loadCurrentShift()),[events,setEvents]=useState<OperationalEvent[]>(()=>StorageService.loadOperationalEvents()),[planBaseline,setPlanBaseline]=useState<PlanBaseline|null>(()=>StorageService.loadPlanBaseline()),[history,setHistory]=useState<FinalizedShiftSnapshot[]>(()=>StorageService.loadHistory());
useEffect(()=>{setRoster(StorageService.loadStaff());setRooms(StorageService.loadRooms());setForecastEvents(StorageService.loadForecast());},[]);useEffect(()=>{localStorage.setItem(PLAN_DATE_KEY,date)},[date]);useEffect(()=>{localStorage.setItem(SHIFT_KEY,shiftType)},[shiftType]);useEffect(()=>{localStorage.setItem(CURRENT_MRS_KEY,String(currentMRS))},[currentMRS]);useEffect(()=>{localStorage.setItem(PROJECTED_MRS_KEY,String(projectedMRS))},[projectedMRS]);
const currentStatus=getMRSStatus(currentMRS),projectedStatus=getMRSStatus(projectedMRS),plannedCensus=rooms.filter(r=>r.isOccupied).length,currentCensus=currentShift.rooms.filter(r=>r.isOccupied).length,currentActiveRNs=currentShift.roster.filter(s=>['RN','Preceptor'].includes(s.role)&&['ACTIVE','RECALLED'].includes(s.staffStatus)).length;const supportRisk=useMemo(()=>{const a:string[]=[];if(currentShift.mtState==='MT_UNFILLED')a.push('MT unfilled');if(currentShift.mtState==='RN_COVERING_MT')a.push('RN covering MT');if(currentShift.pctState==='PCT_NONE')a.push('No PCT');return a},[currentShift.mtState,currentShift.pctState]);
const handleGenerateRecommendation=(teleQuad=allowTeleQuad,chargePatients=chargeTakingPatients)=>{const result=runRecommendationEngine(roster,rooms,chargePatients,teleQuad),updated=rooms.map(r=>({...r,assignedNurseId:result.assignments[r.roomNumber]||null}));setRooms(updated);setWarnings(result.warnings);setFitScore(result.fitScore);setFitLabel(result.fitLabel);setUnassignedRooms(result.unassignedRooms);StorageService.saveRooms(updated)};const saveRoster=(u:NurseStaff[])=>{setRoster(u);StorageService.saveStaff(u)};
const savePlanBaseline=()=>{const b:PlanBaseline={id:`plan-${Date.now()}`,date,shiftType,finalizedAt:new Date().toISOString(),savedAt:new Date().toISOString(),roster,rooms,onCall,fitScore,warnings,mtState,pctState,currentMRS,projectedMRS};StorageService.savePlanBaseline(b);setPlanBaseline(b);window.alert('Tomorrow plan saved as baseline.')};const startCurrentFromPlan=()=>{const p=StorageService.loadPlanBaseline();if(!p){window.alert('Save a Tomorrow Plan baseline first.');setActiveTab('plan');return}const s=StorageService.startCurrentShiftFromPlan(p);setCurrentShift(s);setEvents(StorageService.loadOperationalEvents());setActiveTab('current')};const reset=()=>{StorageService.resetToDefaults();setRoster(StorageService.loadStaff());setRooms(StorageService.loadRooms());setOnCall(StorageService.loadOnCall());setForecastEvents(StorageService.loadForecast());setWarnings([]);setUnassignedRooms([]);setCurrentShift(StorageService.loadCurrentShift());setHistory([]);setActiveTab('current')};const refreshHistory=()=>setHistory(StorageService.loadHistory());const previousShift=history[0]||null;
return <div className="min-h-screen bg-slate-100 flex flex-col font-sans"><header className="bg-slate-900 text-white px-6 py-3 no-print"><div className="max-w-7xl mx-auto flex flex-wrap justify-between items-center gap-4"><div><h1 className="font-black text-lg">1E ASSIGNMENT RECOMMENDATION ENGINE</h1><p className="text-xs text-slate-400">Current operations • Tomorrow planning • Next-shift readiness</p></div><div className="flex gap-2 text-xs"><div className="bg-slate-800 px-3 py-1.5 rounded">Current Census <b>{currentCensus}</b></div><div className="bg-slate-800 px-3 py-1.5 rounded">Current Bedside RNs <b>{currentActiveRNs}</b></div><div className="bg-slate-800 px-3 py-1.5 rounded">Tomorrow Census <b>{plannedCensus}</b></div>{supportRisk.length>0&&<div className="bg-amber-950 px-3 py-1.5 rounded text-amber-200">{supportRisk.join(' • ')}</div>}</div></div></header>
<nav className="bg-white border-b px-6 py-2.5 no-print"><div className="max-w-7xl mx-auto flex justify-between"><div className="flex gap-2 flex-wrap"><button onClick={()=>setActiveTab('current')} className={`px-3 py-1.5 rounded-lg text-xs font-bold flex gap-1 ${activeTab==='current'?'bg-emerald-600 text-white':''}`}><Clock3 className="w-3.5"/>Current Staffing</button><button onClick={()=>setActiveTab('plan')} className="px-3 py-1.5 text-xs font-bold flex gap-1"><CalendarDays className="w-3.5"/>Tomorrow Plan</button><button onClick={()=>setActiveTab('forecast')} className="px-3 py-1.5 text-xs font-bold">Next Shift Readiness</button><button onClick={()=>{refreshHistory();setEvents(StorageService.loadOperationalEvents());setActiveTab('history')}} className="px-3 py-1.5 text-xs font-bold flex gap-1"><History className="w-3.5"/>History</button><button onClick={()=>{refreshHistory();setActiveTab('print')}} className="px-3 py-1.5 text-xs font-bold flex gap-1"><Printer className="w-3.5"/>Print View</button></div><button onClick={reset}><RotateCcw className="w-4"/></button></div></nav>
{activeTab==='plan'&&<section className="bg-white border-b no-print"><div className="max-w-7xl mx-auto px-6 py-3 flex flex-wrap items-end gap-4"><div><label className="text-[10px] font-black uppercase block">Planning Date</label><input type="date" value={date} onChange={e=>setDate(e.target.value)} className="border rounded px-3 py-2"/></div><div><label className="text-[10px] font-black uppercase block">Shift</label><select value={shiftType} onChange={e=>setShiftType(e.target.value as 'Day'|'Night')} className="border rounded px-3 py-2"><option>Day</option><option>Night</option></select></div><div><label className="text-[10px] font-black uppercase block">Current MRS</label><input type="number" step=".001" value={currentMRS} onChange={e=>setCurrentMRS(Number(e.target.value))} className="w-28 border rounded px-3 py-2"/> {currentStatus.color}</div><div><label className="text-[10px] font-black uppercase block">Projected MRS</label><input type="number" step=".001" value={projectedMRS} onChange={e=>setProjectedMRS(Number(e.target.value))} className="w-28 border rounded px-3 py-2"/> {projectedStatus.color}</div><div className="ml-auto flex gap-2"><button onClick={()=>handleGenerateRecommendation()} className="bg-emerald-600 text-white px-3 py-2 rounded text-xs font-black flex gap-1"><Play className="w-3.5"/>Generate Tomorrow Plan</button><button onClick={savePlanBaseline} className="bg-blue-600 text-white px-3 py-2 rounded text-xs font-bold">Save Baseline</button></div></div></section>}
{unassignedRooms.length>0&&activeTab==='plan'&&<div className="bg-rose-600 text-white px-6 py-3 no-print"><div className="max-w-7xl mx-auto flex gap-2"><AlertTriangle className="w-5"/>Rooms {unassignedRooms.join(', ')} require CN review.</div></div>}
<main className="flex-1 p-6 max-w-7xl mx-auto w-full">{activeTab==='current'&&<CurrentStaffing currentShift={currentShift} onCurrentShiftChange={setCurrentShift} events={events} onEventsChange={setEvents} onStartFromPlan={startCurrentFromPlan}/>} {activeTab==='plan'&&<div className="space-y-6"><RosterPlanner roster={roster} mtState={mtState} pctState={pctState} onRosterChange={saveRoster} onMtStateChange={setMtState} onPctStateChange={setPctState} onCall={onCall} onOnCallChange={u=>{setOnCall(u);StorageService.saveOnCall(u)}}/><UnitBoard rooms={rooms} roster={roster} warnings={warnings} onRoomChange={u=>{const n=rooms.map(r=>r.roomNumber===u.roomNumber?u:r);setRooms(n);StorageService.saveRooms(n)}} onAssignRoom={(num,id)=>{const n=rooms.map(r=>r.roomNumber===num?{...r,assignedNurseId:id}:r);setRooms(n);StorageService.saveRooms(n)}}/></div>}{activeTab==='forecast'&&<NextShiftForecast events={forecastEvents} roster={roster} rooms={rooms} mtState={mtState} pctState={pctState} onAddEvent={e=>{const u=[...forecastEvents,e];setForecastEvents(u);StorageService.saveForecast(u)}} onDeleteEvent={id=>{const u=forecastEvents.filter(e=>e.id!==id);setForecastEvents(u);StorageService.saveForecast(u)}}/>}{activeTab==='history'&&<HistoryView plan={planBaseline} current={currentShift} history={history} events={events}/>} {activeTab==='print'&&<div><div className="no-print mb-4 flex justify-between bg-white p-4 rounded border"><span className="text-sm">Current + previous shift handoff sheet. Snapshot the outgoing shift before handoff so it appears on the left.</span><button onClick={()=>window.print()} className="bg-blue-600 text-white px-4 py-2 rounded text-xs font-bold">Print Current Sheet</button></div><PrintSheet date={currentShift.date} shiftType={currentShift.shiftType} roster={currentShift.roster} rooms={currentShift.rooms} onCall={currentShift.onCall} previousShift={previousShift}/></div>}</main></div>;
};
